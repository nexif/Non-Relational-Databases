printjson(
    db.people.aggregate([
        {$group:{
            "_id": "$sex",
            "average_weight" : {$avg: "$weight"},
            "average_height" : {$avg: "$height"}

        }}
    ]).toArray()
)